{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "exports": {
    "./client": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate --schema=prisma/schema.prisma --env-file=../../.env",
    "migrate": "prisma migrate deploy --schema=prisma/schema.prisma --env-file=../../.env",
    "migrate:dev": "prisma migrate dev --schema=prisma/schema.prisma --env-file=../../.env",
    "build": "tsc -b",
    "postinstall": "pnpm prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^6.17.0",
    "@repo/typescript-config": "workspace:*"
  },
  "dependencies": {
    "@prisma/client": "^6.17.0",
    "@repo/backend-common": "workspace:*"
  }
}